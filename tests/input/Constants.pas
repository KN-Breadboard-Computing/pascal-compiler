program Constants;

const
  constInt = -19;
  constUInt = 28;
  constChar = 'P';
  constString = 'Hello, World!';
  constTrue = TRUE;
  constFalse = FALSE;
  constMaxInt = MAXINT;
begin
  writeln(constInt);
  writeln(constUInt);
  writeln(constChar);
  writeln(constString);
  writeln(constTrue);
  writeln(constFalse);
  writeln(constMaxInt);
end.